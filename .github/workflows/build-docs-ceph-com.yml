name: Build docs.ceph.com offline docs

on:
  push:
    branches: [main]
    paths:
      - 'docs-ceph-com/**'
      - '.github/workflows/build-docs-ceph-com.yml'
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository
        uses: actions/checkout@v4

      # Because we are building historical branches in Docker, we must disable
      # fetch-depth: 1 here so the Docker builder has access to the full git history!
      - name: Checkout official Ceph repository
        uses: actions/checkout@v4
        with:
          repository: ceph/ceph
          path: docs-ceph-com/ceph-repo
          fetch-depth: 0

      - name: Prepare tags and environment
        id: prep
        run: |
          cd docs-ceph-com/ceph-repo
          echo "COMMIT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build and push
        uses: ./.github/actions/build-and-push-offline-docs
        with:
          image: ceph-offline-docs
          context: ./docs-ceph-com
          version-tag: ${{ env.COMMIT_SHA }}
