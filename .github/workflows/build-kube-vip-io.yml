name: Build kube-vip.io offline docs

on:
  push:
    branches: [main]
    paths:
      - 'kube-vip-io/**'
      - '.github/workflows/build-kube-vip-io.yml'
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository
        uses: actions/checkout@v4

      # The kube-vip.io docs source lives in the dedicated kube-vip/website
      # repo (a Hugo site), not the main kube-vip/kube-vip repo.
      - name: Checkout official kube-vip website repository
        uses: actions/checkout@v4
        with:
          repository: kube-vip/website
          path: kube-vip-io/kube-vip-src
          fetch-depth: 1

      - name: Prepare tags and environment
        id: prep
        run: |
          cd kube-vip-io/kube-vip-src
          echo "COMMIT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build and push
        uses: ./.github/actions/build-and-push-offline-docs
        with:
          image: kube-vip-offline-docs
          context: ./kube-vip-io
          version-tag: ${{ env.COMMIT_SHA }}
