server {
    listen 80;
    absolute_redirect off;
    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        # Check for exact file, then Next.js .html output, then directory index, then 404
        try_files $uri $uri.html $uri/index.html $uri/ =404;
    }
}